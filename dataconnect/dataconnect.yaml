specVersion: "v1"
serviceId: "bcsithub"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bcsithub-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
